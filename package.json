{
  "name": "1j1s-main-cms",
  "private": true,
  "version": "1.0.0",
  "description": "1 jeune 1 solution main CMS ",
  "scripts": {
    "docker:start": "docker-compose --env-file=.env.docker up -d && sleep 5 && open http://localhost/admin",
    "docker:new-component": "docker-compose stop && docker-compose --env-file=.env.docker up --build -d",
    "docker:start::log": "docker-compose --env-file=.env.docker up -d && docker-compose logs -f",
    "docker:down": "docker-compose --env-file=.env.docker down",
    "docker:clean": "docker-compose --env-file=.env.docker down && docker volume prune",
    "docker:stop": "docker-compose --env-file=.env.docker stop",
    "develop": "strapi develop",
    "dev": "strapi develop",
    "start": "strapi start",
    "build": "strapi build",
    "strapi": "strapi"
  },
  "dependencies": {
    "@strapi/plugin-users-permissions": "4.5.3",
    "@strapi/provider-upload-aws-s3": "~4.5.3",
    "@strapi/strapi": "4.5.3",
    "pg": "8.6.0",
    "strapi-plugin-meilisearch": "~0.8.2",
    "strapi-plugin-slugify": "~2.3.1"
  },
  "author": {
    "name": "A Strapi developer"
  },
  "strapi": {
    "uuid": "c8583f78-6df0-4091-8734-ede822e386b8"
  },
  "engines": {
    "node": ">=14.19.1 <=18.x.x",
    "npm": ">=6.0.0"
  },
  "license": "MIT"
}
